---
title: "Delete version"
api: "DELETE https://api.respan.ai/api/prompts/<prompt_id>/versions/<prompt_version_id>/"
---

You can delete a prompt version by sending a DELETE request to the prompt version endpoint.

## Example

<CodeGroup>
```python example.py
import requests

url = "https://api.respan.ai/prompts/<prompt_id>/versions/<prompt_version_id>/"
api_key = "YOUR_RESPAN_API_KEY" # Replace with your actual Respan API key
headers = {
    "Authorization": f"Bearer {api_key}",
    "Content-Type": "application/json"
}

response = requests.delete(url, headers=headers)
print(response.json())
```

```typescript example.ts
fetch('https://api.respan.ai/prompts/<prompt_id>/versions/<prompt_version_id>/', 
{
  method: 'DELETE',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer YOUR_RESPAN_API_KEY'
    }
})
.then(response => response.json())
.then(data => console.log(data));
```
</CodeGroup>